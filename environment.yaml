name: workshop
channels:
  - conda-forge
  - bioconda
  - defaults
dependencies:
  - python=3.11
  - pymol-open-source
  - reduce
  - pip
  - pip:
      - biopython
      - python-dotenv
      - pandas
      - numpy
      - matplotlib
      - loguru
      - py3dmol
      - requests
      - rdkit